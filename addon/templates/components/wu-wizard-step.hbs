{{#if isActive}}
{{yield
  (hash
  header=(component "wu-wizard-header")
  pane=(component "wu-wizard-pane" nextStep=nextStep finish=finish hasNextStep=hasNextStep)
  )
}}
{{else}}
  {{yield to="inverse"}}
{{/if}}

